<%# app/views/events/index.html.erb - VERSION CLAIRE ET CHALEUREUSE %>
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>SalsaSpot — Trouve ta soirée latino ce soir 🔥</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Bebas+Neue&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary: #ff6b35;
      --primary-glow: rgba(255, 107, 53, 0.3);
      --secondary: #f7931e;
      --accent: #e63946;
      --bg-light: #fafafa;
      --bg-card: #ffffff;
      --bg-elevated: #f5f5f5;
      --text-primary: #1a1a1a;
      --text-secondary: #666666;
      --border: rgba(0, 0, 0, 0.1);
      --shadow: rgba(0, 0, 0, 0.08);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    html, body { height: 100%; overflow: hidden; }

    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--bg-light);
      color: var(--text-primary);
      display: flex;
      flex-direction: column;
    }

    /* NAVBAR */
    .navbar {
      background: var(--bg-card);
      border-bottom: 1px solid var(--border);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-shrink: 0;
      box-shadow: 0 2px 8px var(--shadow);
    }

    .navbar-brand {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      text-decoration: none;
    }

    .logo {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.75rem;
      letter-spacing: 0.05em;
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .tagline {
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .navbar-cta {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.75rem 1.5rem;
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: white;
      text-decoration: none;
      border-radius: 2rem;
      font-weight: 600;
      transition: all 0.3s;
      box-shadow: 0 4px 12px var(--primary-glow);
    }

    .navbar-cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px var(--primary-glow);
    }

    .discover-link {
      color: var(--text-secondary);
      text-decoration: none;
      font-weight: 500;
      font-size: 0.9375rem;
      transition: color 0.2s;
    }

    .discover-link:hover {
      color: var(--primary);
    }

    /* FILTRES */
    .filters {
      background: var(--bg-card);
      border-bottom: 1px solid var(--border);
      padding: 1rem;
      flex-shrink: 0;
      box-shadow: 0 2px 8px var(--shadow);
    }

    .filters-container {
      display: flex;
      gap: 0.75rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .filter-chip {
      padding: 0.625rem 1.25rem;
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      border-radius: 2rem;
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--text-secondary);
      cursor: pointer;
      transition: all 0.3s;
      white-space: nowrap;
    }

    .filter-chip:hover {
      background: #e8e8e8;
      border-color: var(--primary);
    }

    .filter-chip.active {
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      border-color: transparent;
      color: white;
      box-shadow: 0 4px 12px var(--primary-glow);
    }

    /* MAIN LAYOUT */
    .main-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      flex: 1;
      overflow: hidden;
    }

    /* CARTE */
    .map-section {
      position: relative;
      background: #f5f5f5;
      border-right: 1px solid var(--border);
    }

    #map {
      width: 100%;
      height: 100%;
    }

    .map-controls {
      position: absolute;
      bottom: 1.5rem;
      right: 1.5rem;
      z-index: 1000;
    }

    .map-btn {
      width: 56px;
      height: 56px;
      background: white;
      border: 1px solid var(--border);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      cursor: pointer;
      transition: all 0.3s;
      box-shadow: 0 4px 12px var(--shadow);
    }

    .map-btn:hover {
      background: var(--bg-elevated);
      transform: scale(1.05);
      box-shadow: 0 6px 20px var(--shadow);
    }

    /* LISTE */
    .events-section {
      background: var(--bg-light);
      overflow-y: auto;
      padding: 2rem;
    }

    .events-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1.5rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid var(--primary);
    }

    .events-header h2 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 2rem;
      letter-spacing: 0.05em;
      color: var(--text-primary);
    }

    .events-count {
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--text-secondary);
      background: var(--bg-card);
      padding: 0.5rem 1rem;
      border-radius: 2rem;
      border: 1px solid var(--border);
    }

    .events-grid {
      display: grid;
      gap: 1.5rem;
    }

    /* CARTE ÉVÉNEMENT */
    .event-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 1rem;
      padding: 1.5rem;
      transition: all 0.3s;
      cursor: pointer;
      position: relative;
      overflow: hidden;
      box-shadow: 0 2px 8px var(--shadow);
    }

    .event-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--primary), var(--secondary));
      transform: scaleX(0);
      transition: transform 0.3s;
    }

    .event-card:hover {
      border-color: var(--primary);
      transform: translateY(-4px);
      box-shadow: 0 8px 24px var(--shadow);
    }

    .event-card:hover::before {
      transform: scaleX(1);
    }

    .event-card.highlighted {
      border-color: var(--primary);
      box-shadow: 0 0 0 3px var(--primary-glow);
    }

    .event-title {
      font-size: 1.25rem;
      font-weight: 700;
      line-height: 1.3;
      margin-bottom: 0.75rem;
      color: var(--text-primary);
    }

    .event-styles {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
      margin-bottom: 1rem;
    }

    .style-tag {
      padding: 0.25rem 0.75rem;
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      border-radius: 1rem;
      font-size: 0.75rem;
      font-weight: 700;
      color: white;
      text-transform: uppercase;
    }

    .badge {
      padding: 0.25rem 0.625rem;
      border-radius: 0.5rem;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .badge-free {
      background: #e8f5e9;
      color: #2e7d32;
      border: 1px solid #a5d6a7;
    }

    .badge-lessons {
      background: #fff3e0;
      color: #e65100;
      border: 1px solid #ffcc80;
    }

    .event-meta {
      display: grid;
      gap: 0.5rem;
      margin: 1rem 0;
      font-size: 0.875rem;
      color: var(--text-secondary);
    }

    .event-meta-item {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .event-description {
      color: var(--text-secondary);
      line-height: 1.5;
      margin-bottom: 1rem;
      font-size: 0.9375rem;
    }

    .event-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: 1rem;
      border-top: 1px solid var(--border);
    }

    .event-price {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--primary);
    }

    .event-cta {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.75rem 1.5rem;
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: white;
      text-decoration: none;
      border-radius: 2rem;
      font-weight: 600;
      font-size: 0.875rem;
      transition: all 0.3s;
    }

    .event-cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px var(--primary-glow);
    }

    /* SCROLLBAR */
    .events-section::-webkit-scrollbar {
      width: 8px;
    }

    .events-section::-webkit-scrollbar-track {
      background: var(--bg-light);
    }

    .events-section::-webkit-scrollbar-thumb {
      background: #ddd;
      border-radius: 4px;
    }

    .events-section::-webkit-scrollbar-thumb:hover {
      background: #ccc;
    }

    /* LEAFLET POPUP */
    .leaflet-popup-content-wrapper {
      background: white !important;
      color: #1a1a1a !important;
      border-radius: 1rem !important;
      padding: 0 !important;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15) !important;
      border: 1px solid #e0e0e0 !important;
    }

    .leaflet-popup-content {
      margin: 0 !important;
      padding: 1rem !important;
      font-family: 'DM Sans', sans-serif !important;
    }

    .leaflet-popup-tip {
      background: white !important;
      border: 1px solid #e0e0e0 !important;
    }

    .leaflet-popup-close-button {
      color: #666 !important;
    }

    /* FOOTER */
    footer {
      background: var(--bg-card);
      border-top: 1px solid var(--border);
      padding: 0.75rem 1rem;
      margin-top: auto;
    }

    .footer-content {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 2rem;
    }

    .footer-brand {
      flex: 1;
      max-width: 500px;
    }

    .footer-logo {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1rem;
      letter-spacing: 0.05em;
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 0.25rem;
    }

    .footer-description {
      color: var(--text-secondary);
      line-height: 1.3;
      font-size: 0.75rem;
    }

    .footer-section {
      flex-shrink: 0;
    }

    .footer-section h3 {
      font-size: 0.6875rem;
      font-weight: 700;
      margin-bottom: 0.375rem;
      color: var(--text-primary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .footer-links {
      list-style: none;
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .footer-links li {
      position: relative;
    }

    .footer-links li:not(:last-child)::after {
      content: '•';
      position: absolute;
      right: -0.6rem;
      color: var(--border);
    }

    .footer-links a {
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 0.75rem;
      transition: color 0.2s;
      white-space: nowrap;
    }

    .footer-links a:hover {
      color: var(--primary);
    }

    .footer-bottom {
      max-width: 1400px;
      margin: 0.5rem auto 0;
      padding-top: 0.5rem;
      border-top: 1px solid var(--border);
      text-align: center;
      color: var(--text-secondary);
      font-size: 0.6875rem;
    }

    @media (max-width: 768px) {
      footer {
        padding: 0.75rem 0.75rem;
      }

      .footer-content {
        flex-direction: column;
        gap: 0.75rem;
        align-items: flex-start;
      }

      .footer-brand {
        max-width: 100%;
      }

      .footer-links {
        gap: 0.75rem;
      }

      .footer-links li:not(:last-child)::after {
        right: -0.45rem;
      }
    }

    /* RESPONSIVE */
    @media (max-width: 1024px) {
      html, body {
        height: auto;
        overflow: auto;
      }

      .main-container {
        grid-template-columns: 1fr;
        grid-template-rows: auto;
        height: auto;
      }

      .map-section {
        position: relative;
        height: 50vh;
        border-right: none;
        border-bottom: 1px solid var(--border);
      }

      .events-section {
        height: auto;
        overflow-y: visible;
      }
    }

    @media (max-width: 640px) {
      .navbar {
        padding: 0.75rem 1rem;
      }

      .logo {
        font-size: 1.5rem;
      }

      .navbar-cta {
        padding: 0.5rem 1rem;
        font-size: 0.8125rem;
        white-space: nowrap;
      }

      .tagline {
        display: none;
      }

      .filters {
        padding: 1rem 0.5rem;
      }

      .filters-container {
        justify-content: center;
        flex-wrap: wrap;
        padding: 0;
      }

      .filter-chip {
        flex-shrink: 0;
        padding: 0.5rem 0.875rem;
        font-size: 0.8125rem;
      }

      /* Masquer l'indicateur de scroll */
      .filters::after {
        display: none;
      }

      .map-section {
        height: 45vh;
      }

      .events-section {
        padding: 1rem;
      }

      .events-header h2 {
        font-size: 1.5rem;
      }

      .event-card {
        padding: 1.25rem;
      }

      .event-title {
        font-size: 1.125rem;
      }
    }
  </style>
</head>
<body>
  <!-- NAVBAR -->
  <nav class="navbar">
    <%= link_to events_path, class: "navbar-brand" do %>
      <div class="logo">SalsaSpot</div>
      <span class="tagline">🎵 Trouve ta soirée latino</span>
    <% end %>

    <div style="display: flex; gap: 1rem; align-items: center;">
      <%= link_to "✨ Découvrir les danses", discover_path, class: "discover-link" %>
      <%= link_to new_event_path, class: "navbar-cta" do %>
        ➕ Ajouter une soirée
      <% end %>
    </div>
  </nav>

  <!-- FILTRES -->
  <div class="filters">
    <div class="filters-container">
      <button class="filter-chip active" data-style="">🌍 Toutes</button>
      <button class="filter-chip" data-style="salsa">💃 Salsa</button>
      <button class="filter-chip" data-style="bachata">🕺 Bachata</button>
      <button class="filter-chip" data-style="kizomba">❤️ Kizomba</button>
      <button class="filter-chip" data-free="true">💸 Gratuit</button>
      <button class="filter-chip" data-lessons="true">📚 Cours</button>
    </div>
  </div>

  <!-- MAIN -->
  <div class="main-container">
    <!-- CARTE -->
    <div class="map-section">
      <div id="map"></div>
      <div class="map-controls">
        <button class="map-btn" id="locate-btn">📍</button>
      </div>
    </div>

    <!-- LISTE -->
    <div class="events-section">
      <div class="events-header">
        <h2>Soirées à venir</h2>
        <span class="events-count"><%= @events.size %> événements</span>
      </div>

      <div class="events-grid">
        <% if @events.any? %>
          <% @events.each do |event| %>
            <div class="event-card"
                 data-event-id="<%= event.id %>"
                 data-lat="<%= event.latitude %>"
                 data-lng="<%= event.longitude %>">
              <h3 class="event-title"><%= event.title %></h3>

              <div class="event-styles">
                <% event.dance_styles.each do |style| %>
                  <span class="style-tag"><%= style.capitalize %></span>
                <% end %>
                <% if event.has_lessons %>
                  <span class="badge badge-lessons">📚 Cours <%= event.lessons_time %></span>
                <% end %>
                <% if event.is_free %>
                  <span class="badge badge-free">💸 Gratuit</span>
                <% end %>
              </div>

              <div class="event-meta">
                <span class="event-meta-item">
                  📅 <%= l(event.starts_at, format: :short) %>
                </span>
                <span class="event-meta-item">
                  📍 <%= event.venue_name %>, <%= event.city %>
                </span>
                <span class="event-meta-item">
                  <%= event.level == 'tous' ? '⚪️' : event.level == 'debutant' ? '🟢' : event.level == 'intermediaire' ? '🟡' : '🔴' %>
                  <%= event.level.capitalize %>
                </span>
              </div>

              <% if event.description.present? %>
                <p class="event-description"><%= truncate(event.description, length: 100) %></p>
              <% end %>

              <div class="event-footer">
                <div class="event-price"><%= event.price_display %></div>
                <%= link_to event_path(event), class: "event-cta" do %>
                  Voir détails →
                <% end %>
              </div>
            </div>
          <% end %>
        <% else %>
          <div style="text-align: center; padding: 4rem 2rem;">
            <div style="font-size: 4rem; margin-bottom: 1.5rem;">🎵</div>
            <h3 style="font-size: 1.5rem; margin-bottom: 1rem; color: var(--text-primary);">Aucune soirée</h3>
            <p style="color: var(--text-secondary); margin-bottom: 2rem;">Sois le premier à en ajouter une !</p>
            <%= link_to new_event_path, class: "event-cta" do %>
              ➕ Ajouter une soirée
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- FOOTER -->
  <footer>
    <div class="footer-content">
      <div class="footer-brand">
        <div class="footer-logo">SalsaSpot</div>
        <p class="footer-description">
          La plateforme pour découvrir toutes les soirées salsa, bachata et kizomba près de chez toi.
          Rejoins la communauté latino !
        </p>
      </div>

      <div class="footer-section">
        <h3>Navigation</h3>
        <ul class="footer-links">
          <li><%= link_to "Carte des soirées", events_path %></li>
          <li><%= link_to "Ajouter une soirée", new_event_path %></li>
        </ul>
      </div>

      <div class="footer-section">
        <h3>Légal</h3>
        <ul class="footer-links">
          <li><a href="#">Mentions légales</a></li>
          <li><a href="#">Politique de confidentialité</a></li>
          <li><a href="#">CGU</a></li>
          <li><a href="#">Contact</a></li>
        </ul>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; <%= Time.current.year %> SalsaSpot. Fait avec ❤️ pour la communauté latino.</p>
    </div>
  </footer>

  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

  <script>
    const map = L.map('map', { zoomControl: false }).setView([46.603354, 1.888334], 6);

    // Tuiles OpenStreetMap standard (affiche en français pour la France)
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '© <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors',
      maxZoom: 19
    }).addTo(map);

    const markersLayer = L.layerGroup().addTo(map);
    const userLayer = L.layerGroup().addTo(map);

    let allEvents = [];
    fetch('/events.json')
      .then(r => r.json())
      .then(events => {
        allEvents = events;
        renderMarkers(events);
      });

    // Variable globale pour le filtre actif
    let activeFilter = null;

    function renderMarkers(events) {
      markersLayer.clearLayers();

      events.forEach(event => {
        if (!event.latitude || !event.longitude) return;

        const color = event.is_free ? '#4CAF50' : '#ff6b35';

        // Prioriser le style filtré, sinon prendre le premier
        let emoji;
        if (activeFilter && event.dance_styles.includes(activeFilter)) {
          // Si un filtre est actif ET l'événement contient ce style, utiliser son emoji
          emoji = activeFilter === 'salsa' ? '💃' :
                  activeFilter === 'bachata' ? '🕺' :
                  activeFilter === 'kizomba' ? '❤️' : '🎵';
        } else {
          // Sinon, ordre de priorité par défaut
          emoji = event.dance_styles.includes('salsa') ? '💃' :
                  event.dance_styles.includes('bachata') ? '🕺' :
                  event.dance_styles.includes('kizomba') ? '❤️' : '🎵';
        }

        const icon = L.divIcon({
          html: `
            <div style="
              background: ${color};
              color: white;
              width: 50px;
              height: 50px;
              display: flex;
              align-items: center;
              justify-content: center;
              border-radius: 50%;
              font-size: 24px;
              box-shadow: 0 4px 16px rgba(0,0,0,0.25);
              border: 3px solid white;
              cursor: pointer;
              transition: transform 0.2s;
            " onmouseover="this.style.transform='scale(1.1)'" onmouseout="this.style.transform='scale(1)'">
              ${emoji}
            </div>
          `,
          className: 'custom-marker',
          iconSize: [50, 50],
          iconAnchor: [25, 50]
        });

        L.marker([event.latitude, event.longitude], { icon })
          .bindPopup(`
            <div style="min-width: 240px; font-family: 'DM Sans', sans-serif;">
              <strong style="font-size: 1.125rem; display: block; margin-bottom: 10px; color: #1a1a1a; line-height: 1.3;">${event.title}</strong>
              <div style="color: #666; margin-bottom: 6px; font-size: 0.9375rem;">
                <span style="margin-right: 8px;">📍</span>${event.venue_name || event.city}
              </div>
              <div style="color: #666; margin-bottom: 14px; font-size: 0.9375rem;">
                <span style="margin-right: 8px;">💰</span>${event.price_display}
              </div>
              <a href="/events/${event.slug}" style="
                display: inline-block;
                background: linear-gradient(135deg, #ff6b35, #f7931e);
                color: white;
                padding: 10px 20px;
                border-radius: 25px;
                text-decoration: none;
                font-weight: 600;
                font-size: 0.9375rem;
                box-shadow: 0 2px 8px rgba(255, 107, 53, 0.3);
              ">Voir détails →</a>
            </div>
          `)
          .addTo(markersLayer);
      });
    }

    // Géolocalisation
    let watchId = null;
    document.getElementById('locate-btn').addEventListener('click', () => {
      if (watchId) {
        navigator.geolocation.clearWatch(watchId);
        watchId = null;
        userLayer.clearLayers();
        document.getElementById('locate-btn').textContent = '📍';
        return;
      }

      if ('geolocation' in navigator) {
        document.getElementById('locate-btn').textContent = '⏳';

        navigator.geolocation.getCurrentPosition(pos => {
          const { latitude, longitude } = pos.coords;

          userLayer.clearLayers();
          L.circleMarker([latitude, longitude], {
            radius: 10,
            color: '#ffffff',
            fillColor: '#1976D2',
            fillOpacity: 1,
            weight: 4
          }).addTo(userLayer);

          map.flyTo([latitude, longitude], 13);
          document.getElementById('locate-btn').textContent = '🛑';
        }, () => {
          alert('Impossible de te localiser');
          document.getElementById('locate-btn').textContent = '📍';
        });
      }
    });

    // Filtres
    document.querySelectorAll('.filter-chip').forEach(chip => {
      chip.addEventListener('click', () => {
        document.querySelectorAll('.filter-chip').forEach(c => c.classList.remove('active'));
        chip.classList.add('active');

        const style = chip.dataset.style;
        const free = chip.dataset.free;
        const lessons = chip.dataset.lessons;

        // Sauvegarder le filtre actif pour les markers
        activeFilter = style || null;

        let filtered = allEvents;

        if (style) filtered = filtered.filter(e => e.dance_styles.includes(style));
        if (free) filtered = filtered.filter(e => e.is_free);
        if (lessons) filtered = filtered.filter(e => e.has_lessons);

        renderMarkers(filtered);
      });
    });

    // Interaction carte ↔ liste : Clic sur card → zoom carte
    document.querySelectorAll('.event-card').forEach(card => {
      card.addEventListener('click', (e) => {
        // Ne pas trigger si on clique sur le lien "Voir détails"
        if (e.target.closest('.event-cta')) return;

        const lat = parseFloat(card.dataset.lat);
        const lng = parseFloat(card.dataset.lng);

        if (lat && lng) {
          map.flyTo([lat, lng], 15, {
            duration: 1.5
          });

          // Highlight visuel de la card
          document.querySelectorAll('.event-card').forEach(c => c.classList.remove('highlighted'));
          card.classList.add('highlighted');
        }
      });
    });
  </script>
</body>
</html>
